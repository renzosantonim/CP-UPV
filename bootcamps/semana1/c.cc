/*
Lenin tiene monos informáticos que se pasan el dia aporreando el teclado. A lo largo del día han escrito números enteros, carácteres y palabras
que Lenin ha ordenado en categorías para procesarlas. Cada categoria se procesa de una forma diferente:
-Enteros: Multiplicar por 2 los enteros.
-Carácteres: Convertir los carácteres a mayúsculas.
-Palabras: Sustituir todas las vocales de cada palabra por el numero 0.
Procesa e imprime los datos de Lenin.

Input
La entrada consiste de 6 lineas.
-La primera contiene un entero N1 y la segunda línea contiene N1 enteros A(0 ≤ A ≤ 100) separados por espacios.
-La tercera contiene un entero N2 y la cuarta línea contiene N2 carácteres del alfabeto latín B, que es uno de estos carácteres [A - Z, a - z]
(sin contar la ñ) separados por espacios.
-La quinta contiene un entero N3 y la sexta línea contiene N3 strings C(1 ≤ len(C) ≤ 100) separados por espacios.

(1 ≤ N1, N2, N3 ≤ 100)

Output
En orden de entrada, 3 lineas con los datos procesados:
-N1 enteros separados por espacios.
-N2 cáracteres separados por espacios.
-N3 strings separados por espacios.
*/

#include <iostream>
#include <string>
#include <assert.h>
